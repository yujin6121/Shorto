version: '3.8'

services:
  urlshortener:
    build: .
    image: urlshortener:latest
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
    volumes:
      - ./data/app:/app/data/app
    restart: unless-stopped
